{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Glob(*)",
      "Write(*)",
      "Edit(*)",
      "Read(*)"
    ],
    "additionalDirectories": [
      "*"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '[RoundTable] Governance framework active. Rules and hooks loaded.'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash hooks/scripts/check-ticket-exists.sh"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "The user is about to modify a file. Check if the file path contains '.claude/CLAUDE.md', '.claude/TeamDocument/1. Policies/', or '.claude/agents/'. If it matches any of these PROTECTED paths, respond 'yes' ONLY if the user has explicitly authorized policy modifications in this session. Otherwise respond 'no' to block. If the file is NOT protected, respond 'yes' to allow."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash hooks/scripts/log-file-change.sh"
          }
        ]
      }
    ]
  }
}
